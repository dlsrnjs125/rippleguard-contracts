{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.rippleguard.dev/common/event-envelope.schema.json",
  "title": "RippleGuard Event Envelope",
  "description": "Common immutable envelope for RippleGuard events. eventId supports idempotency, correlationId joins one loan application flow, and causationId identifies the immediately preceding event; root events use null.",
  "type": "object",
  "properties": {
    "eventId": { "type": "string", "format": "uuid", "description": "Immutable event identifier used for duplicate detection." },
    "eventType": { "type": "string", "minLength": 1, "description": "Versioned event name, independent from schemaVersion." },
    "schemaVersion": { "type": "string", "pattern": "^[1-9][0-9]*\\.[0-9]+\\.[0-9]+$", "description": "Semantic version of this event schema." },
    "occurredAt": { "type": "string", "format": "date-time", "pattern": "T.*(?:Z|[+-][0-9]{2}:[0-9]{2})$", "description": "ISO-8601 timestamp including a timezone." },
    "producer": { "type": "string", "minLength": 1 },
    "applicationId": {
      "type": ["string", "null"],
      "minLength": 1,
      "description": "Loan Application identifier when the event belongs to an application. Null is allowed for events created before an application exists; legacy v1 payloads may omit it."
    },
    "caseId": { "type": "string", "minLength": 1 },
    "evaluationRunId": {
      "type": ["string", "null"],
      "format": "uuid",
      "description": "Evaluation Run identifier when the event is run-scoped. Null is allowed for pre-evaluation events; legacy v1 payloads may omit it."
    },
    "correlationId": { "type": "string", "minLength": 1 },
    "causationId": { "type": ["string", "null"], "format": "uuid", "description": "Previous event ID, or null only for a root event." },
    "payload": { "type": "object" }
  },
  "required": ["eventId", "eventType", "schemaVersion", "occurredAt", "producer", "caseId", "correlationId", "causationId", "payload"]
}
